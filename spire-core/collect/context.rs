#[derive(Debug, Clone)]
pub struct HandlerContext {
    // Request
    // Response
    // TaskQueue
    // DataQueue xS
}

impl HandlerContext {
    pub fn new(req: (), resp: ()) -> Self {
        todo!()
    }
}
