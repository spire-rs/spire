# https://doc.rust-lang.org/cargo/reference/manifest.html

[package]
name = "spire-core"
version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
readme = "./README.md"

authors = { workspace = true }
repository = { workspace = true }
homepage = { workspace = true }
documentation = { workspace = true }

categories = ["asynchronous", "web-programming"]
keywords = ["crawler", "scraper", "web", "framework"]
description = """
Core types and traits for Spire.
"""

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[lib]
path = "./lib.rs"

[features]
default = ["client", "driver"]
full = ["client", "driver", "tracing", "util"]

client = ["dep:hyper"]
driver = ["dep:fantoccini", "dep:deadpool"]

#time = []
#chrono = []

sqlite = ["dep:rusqlite"]
persy = ["dep:persy"]

tracing = []
util = []

[dependencies]
bytes = { workspace = true }
tower = { workspace = true, features = ["load"] }
async-trait = { workspace = true }
pin-project-lite = { workspace = true }
thiserror = { workspace = true }

http = { version = "1.1.0" }
http-body = { version = "1.0.0" }
http-body-util = { version = "0.1.0" }
time = { version = "0.3.34" }

hyper = { version = "1.2.0", optional = true, features = ["client"] }
hyper-util = { version = "0.1.3", features = ["service"] }

fantoccini = { version = "0.20.0-rc.7", optional = true }
deadpool = { version = "0.10.0", optional = true }

rusqlite = { version = "0.31.0", optional = true, features = [] }
persy = { version = "1.5.0", optional = true, features = [] }
